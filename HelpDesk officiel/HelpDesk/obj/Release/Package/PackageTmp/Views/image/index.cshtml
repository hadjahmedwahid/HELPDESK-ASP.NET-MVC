@model IEnumerable<HelpDesk.PieceJoint>

@{
    ViewBag.Title = "index";
}

<h2>index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.img)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.img)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPiece }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPiece }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPiece })
        </td>
    </tr>
}

</table>
